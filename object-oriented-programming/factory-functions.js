const elf = {
    name: 'Orwell',
    weapon: 'bow',
    attack() {
        return `attack with ${elf.weapon}`;
    }
}

elf.attack();

const elf2 = {
    name: 'Sally',
    weapon: 'bow',
    attack() {
        return `attack with ${elf.weapon}`;
    }
}